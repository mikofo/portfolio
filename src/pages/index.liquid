<!DOCTYPE html>
  <html lang="en">
    <head>
     {% include 'src/templates/head.liquid' %}
      <title>Mike Foster - frontend engineer and product creator</title>
    </head>

    <body>
      {% include 'src/templates/header.liquid' %}
      <main>
        <section class="hero">
            <div class="hero_slot">
              <img src="/assets/logo.png" alt="Mike Foster" class="logo">
            </div>
            <div class="hero_slot">
              <p class="dm-mono" role='doc-subtitle'>frontend engineer & product creator</p>
            </div>
        </section>
      </main>
      <section class='articles'>
      <div class="articles-header">
        <h1>Projects</h1>
        <a href="/projects">See All ></a>
        </div>
        <ul>
          {% for article in projects %}
            {% include 'src/templates/project-link.liquid' with article %}
          {% endfor %}
        </ul>
      </section>
      <section class='articles'>
        <div class="articles-header">
          <h1>Blog</h1>
          <a href="/blog">See All ></a>
        </div>
        <ul>
          {% for article in blog %}
            {% include 'src/templates/blog-link.liquid' with article %}
          {% endfor %}
        </ul>
      </section>
      {% include 'src/templates/footer.liquid' %}
    </body>
  </html>