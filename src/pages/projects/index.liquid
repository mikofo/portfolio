<!DOCTYPE html>
  <html lang="en">
    <head>
     {% include 'src/templates/head.liquid' %}
      <title>Mike Foster - Projects</title>
    </head>

    <body>
      {% include 'src/templates/header.liquid' %}
      <main class="preview-list">
        <h1 class="preview-list__title">Projects</h1>
        <p class="preview-list__subtitle">
          Here are case studies from my favorite projects. Projects range from professional to personal and cover a variety of topics.
        </p>
        <ul class="preview-list__grid">
          {% for article in projects %}
            {% include 'src/templates/project-preview.liquid' with article %}
          {% endfor %}
        </ul>
      </main>
    </body>
  </html>