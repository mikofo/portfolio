<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
</head>
<body>
  <header>
    <h1>{{ title }}</h1>
    <p><em>{{ date }}</em></p>
  </header>
  <main>
    {{ content | raw }}
  </main>
    {% include 'src/templates/footer.liquid' %}
</body>
</html>
